import { FileDto } from "@/types/FileDto";
interface FileStatsProps {
    files: FileDto[];
}
declare const FileStats: ({ files }: FileStatsProps) => import("react/jsx-runtime").JSX.Element;
export default FileStats;
